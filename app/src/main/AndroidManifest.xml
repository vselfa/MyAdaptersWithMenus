<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myadapterswithmenus">

    <application
        android:allowBackup="true"
        android:icon="@drawable/plat_paella_marisc"
        android:label="@string/app_name"
        android:roundIcon="@drawable/plat_paella_marisc"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ASimpleAdapterActivity"></activity>
        <activity android:name=".HandmadeLayoutActivity"></activity>
        <activity android:name=".SimplestAdapterActivity"></activity>
        <activity android:name=".Menjars.MenjarsActivity"></activity>

    </application>

</manifest>